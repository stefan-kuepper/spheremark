server:
  host: "127.0.0.1"
  port: 8000

images:
  # Note: remote_path is now per-project, stored in the database
  allowed_extensions: [".jpg", ".jpeg", ".png"]

thumbnails:
  max_width: 256
  quality: 85

database:
  path: "data/annotations.db"

export:
  default_format: "coco"
  coordinate_precision: 6

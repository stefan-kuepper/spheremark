server:
  host: "0.0.0.0"
  port: 8000

images:
  remote_path: "."
  allowed_extensions: [".jpg", ".jpeg", ".png"]

thumbnails:
  max_width: 256
  quality: 85

database:
  path: "data/annotations.db"

export:
  default_format: "coco"
  coordinate_precision: 6
